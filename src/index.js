import React, { Component } from "react";
import ReactDOM from "react-dom";

import AllTogetherExercise2Exercise from "./App";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <React.StrictMode>
    <AllTogetherExercise2Exercise />
  </React.StrictMode>,
  rootElement
);
